# OS and editor files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env.*.local

# context-mode runtime artifacts
*.db
*.db-shm
*.db-wal

# context-mode session files
.github/context-mode/sessions/

# graphify output
graphify-out/cost.json
graphify-out/cache/
graphify-out/20*

# Clinical events Markdown — the clinical source of truth, written at runtime.
# The pattern carries no leading separator, so it also covers backend/data/ when
# the backend is started from backend/ instead of the repository root.
data/