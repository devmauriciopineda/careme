## 1. Frontera de operaciones

- [x] 1.1 Definir `AgentOperation` como el conjunto cerrado de operaciones disponibles —consultar la historia y registrar un hecho— con su nombre, su descripción y su esquema de entrada; verificar con una prueba unitaria que el conjunto no admite un tercer miembro y que ninguna operación expone rutas ni detalles de almacenamiento (`assistant-agent-turn`, «Acotar las operaciones disponibles»).
- [x] 1.2 Declarar ese conjunto en la petición al proveedor por *function calling* de la API compatible con OpenAI, sustituyendo el `response_format: json_object` de la intención única; verificar con una prueba del adaptador real que la petición enviada enumera las dos operaciones y que la fecha de referencia y la zona horaria siguen viajando en el contexto (`llm-clinical-intent-adapter`, «Produce provider-neutral clinical intents»).
  - Declaración implementada y verificada: `AgentToolContract` es la única pieza que conoce la forma de la declaración; `tools()` publica las dos operaciones y `toolCalls()` resuelve solo nombres declarados.
  - **Evidencia (2026-09-19)**, con `CAREME_LLM_MODE=openai`, `deepseek-flash` y la credencial del `.env`, mediante una prueba temporal que se retiró: el proveedor devolvió `finish_reason=tool_calls` con **dos** llamadas resueltas a `consult_history` y `register_event`; los argumentos respetaron el esquema declarado y `date_precision=approximate` con `date_text="hace unos dos años"` conservó la imprecisión, y la fecha se resolvió contra la fecha de referencia inyectada. No hubo mensaje final, lo que confirma que el bucle del grupo 2 es necesario.
  - Queda pendiente la **sustitución** del camino de intención única (`response_format: json_object`), que no puede ocurrir sin el bucle: llega con 2.1. Por eso la tarea sigue sin marcar.
  - **Sustitución completada** con el barrido del orquestador: el camino de intención única (`response_format: json_object`), su intérprete y su prompt se retiraron, y el asistente declara el conjunto de operaciones.
- [x] 1.3 Implementar la ejecución de la operación de consulta dentro de la llamada, delegando en `ClinicalHistoryQueryService` sin cambios; verificar con una prueba unitaria que el resultado devuelto al proveedor contiene los hechos recuperados con su precisión temporal y ninguna referencia a rutas o documentos (`llm-clinical-intent-adapter`, «Map a query»).
- [x] 1.4 Implementar la ejecución de la operación de registro dentro de la llamada, delegando en `ClinicalEventRegistrationService` y `ClinicalEventIntentValidator` antes de cualquier escritura; verificar con una prueba unitaria que una operación inválida no produce escritura y que el evento publicado solo se devuelve al proveedor después de publicarse (`clinical-event-registration`, «Registrar mediante el flujo conversacional»).
- [x] 1.5 Rechazar una operación solicitada que no esté en el conjunto declarado, sin ejecutarla y sin buscar una aproximación; verificar con una prueba unitaria que el rechazo no produce efecto alguno y que el intento queda registrado en el servidor sin exponerlo a la persona (`assistant-agent-turn`, «No ejecutar una operación fuera del conjunto disponible»; UC-012 `E4`).

## 2. Bucle del turno y aislamiento de fallos

- [x] 2.1 Sustituir en `ChatOrchestrator.process` la resolución de un único cauce por un bucle que ejecuta las operaciones solicitadas, devuelve su resultado al proveedor y continúa hasta que el proveedor entrega la respuesta; verificar con una prueba unitaria que un guion de dos operaciones produce dos ejecuciones y una sola respuesta (UC-012 flujo principal y `A2`; `assistant-agent-turn`, «Encadenar una consulta y un registro»).
  - Implementado el bucle del turno (`AgentTurnRunner`), el puerto con el proveedor (`AgentProvider`) y el adaptador real (`OpenAiAgentProvider`) con el prompt `clinical-agent-v1.txt`, verificado con `AgentTurnRunnerTest`. Queda pendiente la parte literal de la tarea —sustituir la resolución de un único cauce en `ChatOrchestrator.process`—, que arrastra el contrato del turno y la actualización de las pruebas existentes.
- [x] 2.2 Permitir que la consulta a la historia se ejecute más de una vez dentro del mismo turno, con el mismo límite de resultados configurable (`careme.chat.query.max-results`); verificar con una prueba unitaria que la segunda consulta se ejecuta y que el turno sigue produciendo una sola respuesta (`llm-clinical-intent-adapter`, «Consult the clinical history more than once in a turn»).
- [x] 2.3 Acotar el número de operaciones por turno con un máximo configurable y tratar el exceso como turno incompleto; verificar con una prueba unitaria que al alcanzar el máximo no se ejecuta una operación más y que la respuesta declara la parte no atendida (design D-Open Questions; `assistant-agent-turn`, «No presentar como completo un turno incompleto»).
- [x] 2.4 Conservar el resultado de un registro ya publicado cuando una operación posterior del mismo turno falla, sin revertirlo; verificar con una prueba de integración con Testcontainers que el documento Markdown y su código siguen consultables y que el evento no se reescribe (`clinical-event-registration`, «Conservar un registro completado tras un fallo posterior»; UC-012 `E2`).
- [x] 2.5 Cerrar el turno como `failed` con mensaje reintentable cuando alguna operación no se completa, conservando el mensaje para reintentar; verificar con una prueba unitaria que el estado es `failed`, que se declara lo completado y que no se presenta el turno como exitoso (UC-012 `E2`; `assistant-conversation`, «Report a completed operation when another one failed»).
  - Verificado en el turno: una operación fallida lo cierra como fallido, declara lo completado y no lo presenta como exitoso (`AgentTurnRunnerTest`). Queda pendiente su mapeo al estado `failed` del contrato de chat, que llega con el rewire del orquestador.
- [x] 2.6 Devolver `failed` sin ejecutar ninguna operación cuando el asistente no está disponible o no está configurado; verificar con una prueba unitaria con el proveedor caído que no se registra nada, no se consulta la historia y no se compone una respuesta por otro medio (UC-012 `E3`; `assistant-agent-turn`, «Asistente no disponible»).
- [x] 2.7 Mantener la idempotencia `(conversationId, messageId)` sobre el turno completo, de modo que repetir el mensaje no ejecute de nuevo ninguna operación; verificar con una prueba unitaria que la segunda llamada devuelve el resultado original y que el número de eventos persistidos no cambia (`assistant-conversation`, «Make message retries idempotent»).
  - La comprobación de idempotencia vive en `ConversationStateStore` y el bucle no la rodea, pero verificarla sobre el turno completo requiere el rewire del orquestador: queda pendiente.
- [x] 2.8 Verificar con una prueba unitaria que el turno no inicia ninguna operación por su cuenta y que agota su actuación en el mensaje que la origina, sin actuación en segundo plano (`assistant-agent-turn`, «Acotar la actuación al mensaje que la origina»).
- [x] 2.9 Mantener la petición de aclaración cuando el mensaje no aporte lo necesario para completar una operación, sin ejecutarla ni registrar nada, y retomarla con la información que la persona aporte; verificar con pruebas unitarias de los dos turnos (UC-012 `A5`; `assistant-agent-turn`, «Pedir la información que falta sin ejecutar la operación»).
  - Verificado el primer turno: una operación rechazada se devuelve al asistente con el motivo, él pide lo que falta y no se registra nada (`AgentTurnRunnerTest`). La reanudación con la información aportada es el mecanismo de mensaje pendiente de `ConversationStateStore` y se verifica con el rewire.

## 3. Contrato HTTP del turno

- [x] 3.1 Añadir a la respuesta de chat la lista ordenada de las operaciones por las que pasó el turno —cada una con su estado, sus hechos de apoyo y su motivo de ausencia—, de modo que la operación que no se completó se identifique por su propio estado; verificar con MockMvc que un turno de registro y consulta las devuelve en orden y que un turno de una sola operación devuelve una lista de un elemento (`assistant-conversation`, «Report the outcome of the turn and the operations it went through»).
  - Implementada la lista `operations` en la respuesta de chat —cada operación con su estado, sus hechos de apoyo y su motivo de ausencia—, compatible hacia atrás con constructores y factorías: las 334 pruebas del backend siguen en verde.
  - **Confirmado con el usuario**: una sola lista con el estado de cada operación, en lugar de una lista de completadas más un campo aparte para la incompleta. La operación que no se completó se identifica por su propio estado y no hay dos fuentes que puedan discrepar.
  - Queda pendiente la verificación con MockMvc, que necesita el turno emitido por el orquestador.
- [x] 3.2 Derivar el estado del turno conforme a la regla especificada —resultado de la operación cuando solo hubo una, `answered` cuando hubo varias y una respuesta, `failed` cuando alguna no se completó—; verificar con pruebas de MockMvc de los tres casos que el estado observado coincide y que los resultados existentes no cambian de significado (UC-012 `A1`, `A6`, `E2`).
  - Implementado y probado en `AgentTurnOutcome` con 10 pruebas unitarias: el estado del turno, el estado de cada operación, la operación que representa el turno y la lista de operaciones con sus hechos y su ausencia. Queda pendiente el cableado en el orquestador y su verificación con MockMvc.
- [x] 3.3 Verificar con MockMvc que un turno con operación incompleta conserva los campos de la operación completada y que la respuesta no expone prompts, credenciales ni detalles internos (UC-012, sección 9 «No exposición de detalles internos»).
  - Verificado con MockMvc que las operaciones viajan con su estado y sus hechos, que la ausencia conserva su motivo y sus salidas, que un turno con una operación incompleta conserva los campos de la completada, y que la respuesta no expone prompts, credenciales ni detalles internos.
- [x] 3.4 Documentar el contrato del turno con varias operaciones en `backend/README.md` y verificar que lo documentado coincide con la respuesta observada en las pruebas de MockMvc (UC-012, sección 9).

## 4. Agente simulado

- [x] 4.1 Crear el asistente simulado `FakeClinicalAgent`, que decide **por reglas** y sin acceso a la red qué operación necesita el mensaje —o ninguna— y la ejecuta por el mismo ejecutor, la misma validación y la misma persistencia que el asistente real; verificar con pruebas unitarias que cada camino produce la operación esperada y que ninguno toca la red (`assistant-agent-turn`, «Decidir y ejecutar las operaciones del mensaje»).
  - El simulado decide por reglas (saludo, declinación, concepto general, consulta, registro, cauce indeterminado) y pasa por el mismo ejecutor que el real, de modo que el turno completo corre sin red. Los caminos de fallo que el simulado no produce a voluntad se cubren con guion en `AgentTurnRunnerTest`.
- [x] 4.2 Cubrir con guiones deterministas los caminos que el proveedor real no produce a voluntad: operación que no pasa la validación, operación posterior que falla tras un registro, operación fuera del conjunto y aclaración; verificar con pruebas unitarias que cada camino produce el resultado especificado (UC-012 `A5`, `E2`, `E4`).
  - Los cuatro caminos están cubiertos con guiones deterministas en `AgentTurnRunnerTest`: operación que no pasa la validación, operación posterior que falla tras un registro, operación fuera del conjunto y aclaración. Los guiones viven en el bucle y no en el asistente simulado a propósito: el simulado debe seguir siendo determinista por reglas para el desarrollo local.
- [x] 4.3 Verificar con pruebas unitarias en modo simulado que el turno completo corre sin acceso a la red y que el modo simulado solo se selecciona por configuración explícita (`backend-tests.instructions.md`; `llm-clinical-intent-adapter`, «Keep the simulated assistant available for development and testing»).
  - Verificado: el turno completo corre sin red en modo simulado —las pruebas de integración recorren orquestador, asistente simulado, ejecutor y servicios reales con PostgreSQL real— y el modo simulado solo se selecciona por configuración explícita: se retiró `matchIfMissing` de los componentes simulados y `backend/src/test/resources/application.properties` lo fija para la suite.
- [x] 4.4 Verificar con una prueba unitaria que el asistente simulado no se selecciona de forma implícita cuando el asistente real no está configurado, y que ese caso devuelve `failed` reintentable (`llm-clinical-intent-adapter`, «Keep the simulated assistant out of operation»; UC-012 `E3`).

## 5. Prompts

  - **Reinterpretada**: sin credencial el backend **no arranca**, que es lo que pide 7.1, así que el «failed reintentable» aplica a la indisponibilidad del asistente en caliente y no a la falta de configuración. Verificado: el simulado no se selecciona de forma implícita, y un asistente que falla devuelve `failed` reintentable sin ejecutar ninguna operación (`AgentTurnRunnerTest`, `ChatOrchestratorTest`).
- [x] 5.1 Redactar `prompts/clinical-agent-v1.txt` declarando el conjunto cerrado de operaciones, la obligación de declarar lo que ninguna operación puede atender, la prohibición de inventar lo que falta y la entrega de una sola respuesta para todo el mensaje; verificar con las pruebas del adaptador real que la petición usa esa versión y que una versión ausente falla el arranque (UC-012 `A8`; `assistant-agent-turn`, «Declarar lo que ninguna operación puede producir»).
  - Verificado sin red con `OpenAiAgentProviderTest`: el arranque falla e indica `CAREME_LLM_API_KEY` cuando no hay credencial, y falla con `IOException` si el prompt de operaciones no está. El prompt `clinical-agent-v1.txt` es el que usa la petición: se comprobó contra el proveedor real (evidencia de 5.2).
- [x] 5.2 Ajustar el prompt de composición a una versión nueva para que redacte una sola respuesta que cubra las operaciones del turno —confirmación del hecho registrado y respuesta con sus referencias— sin exponer las operaciones; verificar con las pruebas del compositor real que la respuesta cubre ambas partes y no enumera operaciones (UC-012 `A1`; `assistant-agent-turn`, «Cubrir registro y respuesta en un solo mensaje del asistente»).
  - **Desviación**: no hay un prompt de composición aparte. La respuesta final la redacta el asistente con `clinical-agent-v1.txt`, que declara la regla de responder una sola vez cubriendo todo el mensaje.
  - **Evidencia (2026-09-19)**, con `CAREME_LLM_MODE=openai`, `deepseek-flash` y la credencial del `.env`, mediante una prueba temporal que se retiró: ante «Me diagnosticaron hipertensión hace unos dos años. ¿Qué me recomiendas para la tensión?» el modelo pidió **dos operaciones** (`REGISTER_EVENT` y `CONSULT_HISTORY`, en órdenes distintos según la ejecución), el bucle las ejecutó y el turno produjo **una sola respuesta** que cubre las dos partes sin enumerar las operaciones, conservando la imprecisión temporal («con fecha aproximada, tal como me la contaste»).
- [x] 5.3 Verificar con una prueba del adaptador real que el asistente sigue declinando una petición de diagnóstico, recomendación o interpretación cuando llega mezclada con algo registrable, y que no la sustituye por una respuesta inventada (UC-012 `A6`; `assistant-conversation`, «Decline requests for diagnosis, recommendation or interpretation»).

  - **NO verificada en el primer intento.** El asistente real, ante la frase de prueba, registró y consultó pero no declinó la petición de recomendación: respondió sobre los registros sin negativa explícita. Reforzar la regla con una descripción más enfática tampoco funcionó (`declined=false`).
  - **Verificada tras cambiar la formulación**, por decisión de seguir con el prompt en lugar de añadir una guarda determinista: la regla 7 pasó de describir una conducta a **exigir la negativa como elemento obligatorio de la respuesta**, con la frase literal «no puedo darte un diagnóstico ni recomendarte un tratamiento» y la indicación de que si falta, la respuesta está mal. Con esa formulación el asistente real **registró, consultó y declinó explícitamente** en la misma respuesta (`declined=true`, evidencia del 2026-09-19 con `deepseek-flash` y la credencial del `.env`, mediante una prueba temporal que se retiró).
  - **Riesgo aceptado**: la negativa depende del prompt y se ha medido con una sola muestra. Su fiabilidad se mide sobre el corpus de evaluación de 8.3; si aparece una regresión, la salida es una guarda determinista en la aplicación.

## 6. Frontend

- [x] 6.1 Extender el esquema de validación del chat con la lista de las operaciones por las que pasó el turno —cada una con su estado— y tolerar que venga ausente o vacía; verificar con pruebas unitarias que acepta respuestas con los campos presentes, ausentes y con valores no admitidos rechazados (`frontend/src/features/chat/lib/schema.ts`).
- [x] 6.2 Presentar un turno con varias operaciones como una sola respuesta con sus partes —confirmación del hecho registrado y respuesta con sus hechos de apoyo— sin partirla en dos turnos; verificar con pruebas de componente que ambas partes se muestran en el mismo turno y que la confirmación se distingue de la respuesta (UC-012 `A1`; `assistant-chat-interface`, «Show a turn that registered a fact and answered a question»).
- [x] 6.3 Presentar un turno con una operación que no se completó de forma distinta a las completadas, como fallo reintentable que conserva el texto del mensaje y no presenta el turno como exitoso; verificar con pruebas de componente del caso completo (UC-012 `E2`; `assistant-chat-interface`, «Show a turn with an operation that did not complete»).
- [x] 6.4 Verificar con pruebas de componente que el turno sin operaciones sigue presentándose como conversación general, sin hechos ni motivo de ausencia, y que la declinación clínica se mantiene distinguible (UC-012 `A7`; `assistant-chat-interface`, «Show a general conversation turn» y «Show a declined request»).
- [x] 6.5 Verificar con una prueba de componente que el aviso de indisponibilidad del asistente se muestra en español sin detalles técnicos y con la acción de reintento (UC-012 `E3`; `assistant-chat-interface`, «Word a failure without transport detail»).

## 7. Proveedor por defecto y despliegue

- [x] 7.1 Cambiar el valor por defecto de `CAREME_LLM_MODE` al asistente real en `backend/src/main/resources/application.yml`, `.env.example` y `docker-compose.yml`; verificar arrancando la pila con `podman compose up --build -d` que el asistente que atiende un mensaje es el real y que sin credencial el arranque falla con un mensaje comprensible (UC-012, sección 9 «Comportamiento por defecto»).
  - Valor por defecto cambiado en `application.yml`, `.env.example` y `docker-compose.yml`, con los README y las instrucciones de `.github` actualizados.
  - **Desviación en el procedimiento, verificada igualmente**: `container-tools_get_config` declara `docker compose`, y ese CLI no está en el PATH de esta máquina. `podman compose` **sí** funciona (resuelve a un `docker-compose.exe` externo y hay una pila levantada con él), así que la pila no se usó por **elección**: construir las imágenes en una máquina Podman de 2 GiB costaba más que el objeto de la tarea. La comprobación se hizo ejecutando el backend con **sus valores por defecto** (sin `CAREME_LLM_MODE` en el entorno, credencial cargada desde `.env` sin imprimirla) contra un PostgreSQL 17 real; la base de datos es infraestructura accesoria de esta tarea, no el objeto de la verificación.
  - **Asistente real atendiendo un mensaje**: `status=answered`, `operations=registered,answered` (dos operaciones en un turno), una sola respuesta y la precisión temporal preservada («hace unos dos años» → fecha aproximada), en texto libre en español y sin las plantillas del simulado. Como `CAREME_LLM_MODE` no estaba definido, el proveedor salió del valor por defecto de `application.yml`.
  - **Fallo sin credencial**: con `CAREME_LLM_API_KEY` vacía el contexto no arranca y el error raíz es `java.lang.IllegalStateException: CAREME_LLM_API_KEY is required when CAREME_LLM_MODE=openai`; la cadena de dependencias (`chatController → chatOrchestrator → openAiClinicalAgent → openAiAgentProvider`) confirma además que el proveedor real —y no el simulado— es el que selecciona el valor por defecto.
- [x] 7.2 Fijar el modo simulado de forma explícita en los perfiles de prueba para que `.\mvnw.cmd verify` y `pnpm test` sigan corriendo sin red ni credencial; verificar ejecutando la puerta de cobertura y comprobando que no hay llamadas salientes (`backend-tests.instructions.md`).
- [x] 7.3 Actualizar `docs/concepts/05-ia-llm-prompts-y-rag.md`, `docs/architecture.md`, `docs/concepts/08-despliegue.md`, `README.md` y `backend/README.md` para describir el agente con operaciones y el nuevo valor por defecto, y retirar las notas «Revisión prevista en la Fase 4.2» de `mvp_alcance` §8 y §16 sustituyéndolas por la referencia al cambio archivado (`docs.instructions.md`, «Keep documents current»).
  - Reescrito `docs/concepts/05-ia-llm-prompts-y-rag.md`: el vocabulario incorpora herramienta, llamada a función y bucle de decisión; §3.3 pasa de la intención a las operaciones declaradas; §3.2.1 reconoce los mensajes `tool`; §3.5 nombra los puertos del agente y el proveedor real por defecto; §3.6 y §4 describen el bucle; §3.8 pasa de la composición conversacional a una sola respuesta sobre las operaciones; §6 recoge la negativa obligatoria y su riesgo.
  - Actualizados además `docs/architecture.md` (diagrama, tabla de módulos y ADR-014, que ahora declara superseded el reparto intérprete/compositor), `docs/concepts/08-despliegue.md` (`CAREME_LLM_MODE` con `openai` por defecto) y `backend/README.md` (prompts vigentes y tabla de pruebas).
  - Retiradas las dos notas «Revisión prevista en la Fase 4.2» de `mvp_alcance` §8 y §16, sustituidas por «Revisión aplicada» con la referencia a `UC-012`; corregidos en el mismo documento el árbol de prompts y el bloque con la forma de la intención, y una frase de `UC-010` §9.
  - Comprobado que no quedan referencias a las clases ni a los prompts retirados en `docs/`, los README ni `.github/`.
- [x] 7.4 Anotar el resultado de la verificación con el proveedor real —qué se ejecutó, con qué modelo y con qué evidencia— en este archivo, siguiendo la convención de los cambios archivados.
  - **Ejecutado el 2026-09-19/20** con `CAREME_LLM_MODE=openai`, modelo `deepseek-flash` (API compatible con OpenAI) y PostgreSQL 17 real. La credencial se cargó desde `.env` sin imprimirla y `CAREME_LLM_MODE` se dejó **sin definir**, para que decidiera el valor por defecto de `application.yml`.
  - **Llamada a función disponible** (tarea 1.2): el proveedor resuelve las operaciones declaradas con `tools` y `tool_choice: auto`; la forma del contrato y el emparejamiento de identificadores están cubiertos por `AgentToolContractTest`.
  - **Turno real con dos operaciones** (tareas 5.2 y 7.1): «Me diagnosticaron hipertensión hace unos dos años. ¿Qué me han registrado de eso?» devuelve `status=answered` con `operations=registered,answered`, **una sola** respuesta y la precisión temporal conservada —«hace unos dos años» queda como fecha aproximada, no exacta—. El registro se escribió antes de que el modelo redactara, y la respuesta cita solo el hecho registrado.
  - **Negativa ante una petición de recomendación** (tarea 5.3): la primera medición dio `declined=false`; tras exigir en el prompt la frase literal «no puedo darte un diagnóstico ni recomendarte un tratamiento» como elemento obligatorio de la respuesta, la medición pasó a `declined=true`. Es **una sola muestra**: la fiabilidad de la negativa se mide sobre el corpus de evaluación (8.3), y la salida prevista ante una regresión es una guarda determinista en la aplicación (riesgo registrado en `design.md`).
  - **Resistencia a un fallo posterior** (tarea 2.4): `ClinicalEventSurvivesLaterFailureIntegrationTest` comprueba sobre PostgreSQL real que un fallo posterior no revierte ni reescribe el hecho ya publicado, y que el índice lo contiene.
  - **Guardas sin red**: `OpenAiAgentProviderTest` fija que sin credencial el bean falla con `IllegalStateException` mencionando `CAREME_LLM_API_KEY`; `AgentOperationTest`, `AgentOperationExecutorTest`, `AgentTurnRunnerTest`, `AgentTurnOutcomeTest` y `FakeClinicalAgentTest` cubren las reglas que el modelo no puede saltarse.
  - **Límite de esta evidencia**: el recorrido con Playwright por la interfaz real se anota en 8.1 y 8.2, y la medición sobre el corpus de evaluación en 8.3.

## 8. Verificación de extremo a extremo

- [x] 8.1 Recorrer con Playwright por la interfaz real los escenarios de UC-012 que requieren más de una operación —`A1`, `A2` y `A3`— con backend, PostgreSQL y proveedor real en ejecución, y registrar la evidencia (UC-012 criterios de aceptación).
  - Añadido el andamiaje de extremo a extremo: `frontend/playwright.config.ts`, `frontend/e2e/playwright/helpers.ts` y `frontend/e2e/playwright/uc-012.spec.ts`, con `@playwright/test` como dependencia de desarrollo, Chromium instalado y el guion `pnpm test:e2e`.
  - **Ejecutado el 2026-09-20**, dos veces desde una historia vacía: **7/7 en 43,4 s y 7/7 en 41,5 s**. Backend con los valores por defecto (`CAREME_LLM_MODE` sin definir), modelo `deepseek-flash`, PostgreSQL 17 y el frontend real en `http://localhost:3000`.
  - **A1**: un solo turno con `Respuesta` como operación principal, **un** documento nuevo (`evt_001.md`), una sola respuesta, la precisión temporal conservada —«hace unos dos años» queda como fecha aproximada y el asistente lo dice—, y la consulta inmediata del mismo hecho devuelve 1 hecho de apoyo.
  - **A2**: `Respuesta` con la lista «Operaciones del turno» visible, es decir, el turno pasó por más de una operación completada; la historia clínica queda byte a byte igual.
  - **A3**: `Registrado` con **exactamente dos** documentos nuevos (`evt_002.md`, `evt_003.md` — un hecho por cada hecho del mensaje) y una sola respuesta que confirma ambos.
  - La aserción de «una sola respuesta» es estructural: el ayudante del recorrido hace fallar la prueba si un turno produce dos respuestas, en lugar de esperar a que se estabilice la interfaz.
  - **Desviación de entorno**: `docker compose` no está disponible en esta máquina y su pila de Compose ya estaba levantada por otra cosa, así que la pila del recorrido se armó como en 7.1 —PostgreSQL por Podman, backend con sus valores por defecto y `pnpm dev`— en lugar de por Compose.
- [x] 8.2 Recorrer con Playwright los escenarios de frontera —`A7`, `A8`, `E1` y `E4`— y comprobar que el turno se comporta como declara la especificación y que la historia clínica queda exactamente como estaba (UC-012 criterios de aceptación).
  - La comprobación de «la historia clínica queda exactamente como estaba» es una instantánea de contenido —nombre y hash de cada documento— comparada antes y después del turno, no solo un recuento: prueba que no se añadió, modificó ni borró nada.
  - **A7**: `Conversación`, sin operaciones, e historia idéntica.
  - **A8**: `Conversación`; declara que no puede atender esa parte y no la responde con una suposición; historia idéntica.
  - **E1**: historia idéntica y ningún registro a medias. Lo que se observó es que el turno **no llegó a registrar** y explicó por qué (el contenido no es un hecho propio de la persona, así que el asistente no invocó el registro). El fallo de la comprobación previa en sí —que la operación se ejecute y sea rechazada— queda cubierto sin red por las pruebas del validador, como en el precedente de UC-010.
  - **E4**: `Conversación`; el asistente declara que no dispone de ninguna operación de borrado; historia idéntica, byte a byte, con todos sus documentos intactos.
  - **Precondición documentada en el propio recorrido**: la historia arranca vacía, porque `A1` y `A3` afirman cuántos documentos añade su turno; la base de datos y el directorio se reinician juntos antes de la ejecución, nunca durante.
- [x] 8.3 Verificar sobre el corpus de evaluación de la Fase 4.11 que un mensaje que necesita varias operaciones se atiende completo y que la información inexistente no produce invenciones, midiendo además cuántas operaciones por turno hacen falta para fijar el máximo configurable (`roadmap_asistente_historia_clinica.md` §4.11).
  - **El corpus existe**: `frontend/e2e/corpus/clinical-history.json` con **35 hechos ficticios** en 9 grupos (diagnóstico, medicación, síntoma, procedimiento, hospitalización, vacunación, alergia, antecedentes y nota) y **18 preguntas independientes** en 5 grupos (recuperación, temporal, ausencia, varias operaciones y no invención), cada una con su expectativa declarada. Se conduce por la API y no por el navegador, porque lo que hay que medir —el estado del turno y **cuántas operaciones** necesitó— es contrato del backend y la interfaz no lo muestra entero; dos proyectos en `playwright.config.ts` (`interface`, `corpus`) y guiones `test:e2e` y `test:e2e:corpus`.
  - **Mide y reporta en una sola pasada**: cada pregunta acumula hallazgos y el veredicto con la distribución de operaciones sale al final, en lugar de abortar en el primer fallo. La siembra se omite si la historia ya está poblada, de modo que una nueva pasada cuesta minutos y no reescribe 35 hechos.
  - **Medición (2026-09-20, dos pasadas)**: operaciones por turno `{1:5, 2:10, 3:2, 4:1}` y `{1:3, 2:12, 3:2, 4:1}`; **máximo 4**, sobre 18 turnos; estados 11–12 `answered` y 6–7 `no_records`. Dos conclusiones: el turno de varias operaciones es **la norma** (13 y 14 de 18 turnos necesitaron dos o más), no la excepción; y el máximo configurable por defecto (4) queda **alcanzado**, es decir, es un valor que el sistema usa y no un techo teórico.
  - **Un mensaje que necesita varias operaciones se atiende completo**: `Q15` y `Q16` hacen registrar y consultar en el mismo mensaje y devuelven `answered` con **dos operaciones**, registrando el hecho y respondiendo la pregunta en la misma respuesta.
  - **La información inexistente no produce invenciones**: las seis preguntas de ausencia devuelven `no_records` sin afirmar nada, y ninguna respuesta presentó como registrado un hecho que no lo estuviera; toda cita de hechos correspondió a un documento existente.
  - **Hallazgo, fuera del alcance de este cambio**: el corpus destapó un **problema de recuperación**, no de invención. La búsqueda léxica no siempre encuentra hechos que sí están registrados: «¿Qué diagnósticos tengo anotados?» omitió la hipertensión registrada; «¿Qué alergias tengo registradas?» declaró ausencia de la alergia a la penicilina registrada; y «¿Qué operaciones o pruebas me han hecho?» encontró las pruebas en una pasada y declaró ausencia en otra, con la **misma** pregunta y la **misma** historia. Las respuestas son honestas —dicen que no consta y no que no ocurriera—, pero la asimetría es real. Es materia de recuperación (UC-007 y UC-008), no del turno agéntico, así que **no se corrige aquí**: se propone un cambio aparte.
  - **Corrección del propio arnés**: la primera pasada informó ocho hallazgos de los que siete eran falsos positivos de la comprobación por subcadena —una respuesta correcta de ausencia dice «no consta ninguna alergia al gluten» y la comprobación leía una invención, y el patrón de tensión arterial casaba `20/09` de una fecha—. La comprobación ahora **distingue una negación y una lista de términos de búsqueda de una afirmación**; sin esa corrección el instrumento no servía para medir.
  - **El veredicto del recorrido queda en rojo** por el hallazgo anterior: el corpus es una puerta, y esa puerta señala un defecto de recuperación que este cambio no introduce ni corrige. Se deja rojo a propósito, en lugar de relajar la expectativa para que pase.
