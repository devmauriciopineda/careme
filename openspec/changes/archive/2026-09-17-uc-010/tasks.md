## 1. Composición conversacional

- [x] 1.1 Crear `ClinicalConversationComposer` como interfaz neutral con el mensaje y los turnos recientes como única entrada; verificar con una prueba unitaria que la entrada no admite hechos clínicos ni referencias, de modo que el compositor no pueda citar registros (UC-010, sección 8 «Reglas de negocio»).
- [x] 1.2 Redactar `prompts/conversation-reply-v1.txt` con las reglas del límite clínico: responder en español como conversación; explicar conceptos generales sin aplicarlos al caso del usuario, sin interpretar sus datos y sin recomendar tratamiento; declinar peticiones de diagnóstico, recomendación o interpretación; no afirmar nada sobre la historia ni presentarlo como registrado; verificar con pruebas del compositor real que cada regla se refleja en la réplica y que una petición de recomendación produce una negativa explícita (UC-010 `A1`, `A5`).
  - El prompt declara las cuatro reglas y el compositor real lo envía como mensaje de sistema; el cumplimiento en la réplica real (no en modo `fake`) se comprueba en la tarea 7.4, y la negativa explícita está cubierta de forma determinista en modo `fake`.
- [x] 1.3 Implementar `OpenAiClinicalConversationComposer` con la versión de prompt y la telemetría correlacionada que ya usa el compositor de respuestas; verificar con una prueba que una respuesta ilegible o un proveedor no disponible produce `LlmIntegrationException` y no una réplica vacía ni inventada (UC-010 `E1`).
- [x] 1.4 Implementar `FakeClinicalConversationComposer` para el modo `fake` y seleccionarlo con `CAREME_LLM_MODE` como los demás adaptadores; verificar con una prueba en modo `fake` que la réplica se compone sin acceso a la red (UC-010 `A1`, `A4`).
- [x] 1.5 Verificar con pruebas unitarias que la réplica de una explicación conceptual no aplica el concepto al caso del usuario, no interpreta sus datos y no recomienda tratamiento (UC-010 `A1`).

## 2. Rama conversacional del orquestador

- [x] 2.1 Sustituir el texto constante de la rama `CONVERSATION` por la invocación al compositor conversacional con el mensaje y `recentTurns()`; verificar con una prueba unitaria que la réplica del compositor es la que devuelve el turno y que `message` ya no es el acuse fijo (UC-010, flujo principal).
- [x] 2.2 Verificar con una prueba unitaria, con dobles de `ClinicalHistoryQueryService` y del repositorio de consulta, que la rama `CONVERSATION` no registra ninguna interacción con la historia clínica (UC-010, flujo principal paso 3, `A4`, `A6`).
  - Se añade además una prueba de integración con doble de espía sobre `ClinicalEventQueryRepository` que exige cero interacciones.
- [x] 2.3 Verificar con pruebas unitarias que el turno conversacional no adjunta hechos ni motivo de ausencia y que su estado es `general_conversation` (UC-010, flujo principal paso 5 y criterio de frontera con UC-008).
- [x] 2.4 Mapear un fallo del compositor conversacional al resultado `failed` con mensaje reintentable y conservación del mensaje; verificar con una prueba unitaria que el turno sale como `failed`, que no se buscó en la historia y que el mensaje queda disponible para reintentar (UC-010 `E1`).
- [x] 2.5 Verificar con una prueba unitaria que un mensaje conversacional que se apoya en hechos mencionados en la conversación activa se responde como conversación general, con los turnos recientes disponibles para el compositor y sin tratarlos como registros (UC-010 `A6`).
- [x] 2.6 Verificar con una prueba unitaria que la redirección del peso y la circunferencia abdominal conserva su mensaje determinado y no pasa por el compositor conversacional (UC-010, frontera con UC-007 y fuera de alcance).

## 3. Clasificación del cauce

- [x] 3.1 Crear `prompts/clinical-intent-v3.txt` con las reglas nuevas —`query` cuando alguna parte dependa de la historia, `general_part` para la parte general de un mensaje mixto, `clarification` cuando no pueda determinarse el cauce, `conversation` solo para lo que no se registra ni depende de la historia— y apuntar el adaptador real a esa versión; verificar con las pruebas del adaptador que interpreta las cuatro formas y que una regla ausente o un valor desconocido no rompe la interpretación (UC-010 `A2`, `A3`, `A7`).
- [x] 3.2 Verificar con una prueba del adaptador real que una reformulación coloquial de algo registrado devuelve `query` y no `conversation`, y que la parte general de un mensaje mixto queda fuera de `search_terms` (UC-010 `A2`, `A3`).
- [x] 3.3 Verificar con una prueba del adaptador real que una pregunta cuya dependencia de la historia no puede determinarse devuelve `clarification` con una pregunta en español, y no `query` ni `conversation` (UC-010 `A7`).
- [x] 3.4 Extender `FakeClinicalIntentInterpreter` para que el modo `fake` distinga los cuatro casos, incluida la duda de cauce y la reformulación coloquial; verificar con pruebas unitarias que cada caso se clasifica como corresponde sin acceso a la red (UC-010 `A2`, `A3`, `A7`).

## 4. Parte general de un mensaje mixto

- [x] 4.1 Añadir `generalPart` opcional a `ClinicalEventIntent.Query` y exigir en el constructor compacto del intent que solo `QUERY` pueda llevarlo; verificar con pruebas unitarias que el campo es opcional, que está ausente en un intent de registro y que un `generalPart` presente no entra en `search_terms` (UC-010 `A2`).
- [x] 4.2 Componer la parte general del mensaje mixto con el compositor conversacional y el resultado de la historia con `ClinicalHistoryQueryService.answer()`; verificar con una prueba unitaria que el estado del turno es el de la historia y que la parte general viaja en su propio campo, sin fundirse en una misma afirmación (UC-010 `A2`).
- [x] 4.3 Verificar con pruebas unitarias que la parte general no se usa como criterio de búsqueda, que no responde la parte clínica cuando la historia declara una ausencia y que no se presenta como apoyo de la respuesta fundada (UC-010 `A2` y criterio de frontera con UC-007 y UC-008).
- [x] 4.4 Verificar con una prueba unitaria que un fallo del compositor en un mensaje mixto conserva el resultado de la historia, omite la parte general y no modifica la historia clínica (UC-010 `E1`).
- [x] 4.5 Verificar con una prueba unitaria que la reformulación coloquial de un hecho registrado se atiende como consulta de la historia y no produce una respuesta conversacional (UC-010 `A3`).
- [x] 4.6 Verificar con una prueba unitaria que una pregunta de cauce indeterminado pide aclaración, no busca en la historia y no responde como conversación general, y que la respuesta del usuario al turno de aclaración se interpreta junto con el mensaje pendiente (UC-010 `A7`).

## 5. Contrato HTTP

- [x] 5.1 Añadir el campo opcional `generalReply` a la respuesta de chat; verificar con MockMvc que un turno mixto lo incluye con el estado de la historia y que `general_conversation`, `answered`, `no_records` y `failed` lo omiten o lo devuelven nulo sin cambiar su estado (UC-010 `A2`).
- [x] 5.2 Verificar con MockMvc que un turno con parte general mantiene `absenceReason` y `suggestedActions` cuando corresponde, y que el turno no expone prompts, credenciales ni detalles internos (UC-010 `A2`, sección 9 «Sin detalles internos»).
- [x] 5.3 Documentar el campo y el comportamiento de `general_conversation` en `backend/README.md` y verificar que el contrato documentado coincide con la respuesta observada en las pruebas de MockMvc (UC-010, sección 9 «Coherencia de idioma»).

## 6. Frontend

- [x] 6.1 Extender el esquema de validación del chat con `generalReply` como campo opcional y nulo; verificar con pruebas unitarias que acepta respuestas con el campo presente, con él ausente y con valores no admitidos rechazados (`frontend/src/features/chat/lib/schema.ts`).
- [x] 6.2 Presentar un turno con parte general como dos bloques —conversación y resultado clínico— y un turno `general_conversation` como conversación sin hechos ni motivo de ausencia; verificar con pruebas de componente que las dos clases de respuesta se distinguen visualmente y que los hechos de apoyo solo aparecen en la parte clínica (UC-010, sección 9 «Separación estricta»).
- [x] 6.3 Verificar con una prueba de componente que una declinación se muestra como conversación y no como respuesta fundada, y que la interfaz no añade consejo clínico propio (UC-010 `A5`).
- [x] 6.4 Verificar con pruebas de componente que el nuevo contenido tiene nombre accesible y que no se exponen detalles internos ni el texto del proveedor (UC-010, sección 9 «Sin detalles internos»).

## 7. Verificación integrada

- [x] 7.1 Añadir una prueba de integración que recorra los desenlaces de UC-010 (pregunta general, explicación conceptual, mensaje mixto, pregunta coloquial, cortesía, declinación, cauce indeterminado y fallo del compositor) y verificar que en todos ellos el número de eventos y el contenido del índice permanecen sin cambios (UC-010, sección 7 «Postcondiciones»).
- [x] 7.2 Ejecutar `.\mvnw.cmd verify` desde `backend/` y verificar que todas las pruebas pasan y que se supera la puerta de cobertura de JaCoCo (o registrar la medición de la puerta si sigue siendo previa al cambio).
  - Las 232 pruebas pasan. La puerta de ramas sigue sin cumplirse y el incumplimiento es previo a este cambio, medido en esta misma máquina: en `HEAD` `7ae31d8` la proporción de ramas es 0,706 y la de líneas 0,915; con `uc-010` son 0,726 y 0,923. La regla de líneas (0,90) se cumple en ambos casos; la regla de ramas (0,90) incumple en ambos, y este cambio la mejora en 0,020.
- [x] 7.3 Ejecutar `pnpm test`, `pnpm lint` y `pnpm typecheck` desde `frontend/` y verificar que todos pasan.
- [x] 7.4 Comprobar el funcionamiento del asistente con el modelo de lenguaje configurado (no en modo `fake`) y anotar la evidencia en esta tarea: una pregunta general y una explicación conceptual obtienen `general_conversation` con respuesta propia y sin eventos; una cortesía obtiene una réplica breve; una petición de recomendación o interpretación obtiene `general_conversation` con una negativa explícita; una pregunta que mezcla lo general con lo propio obtiene la parte general en `generalReply` y la parte clínica por su cauce; una pregunta coloquial sobre algo registrado obtiene `answered` con su hecho; una pregunta de cauce indeterminado obtiene `clarification_required`; y en ninguno de los casos conversacionales se leen eventos (UC-010 `A1`–`A7`, `E1`).
  - **Ejecutado el 2026-09-17** con `CAREME_LLM_MODE=openai`, `deepseek-flash` y PostgreSQL 17 real, mediante una prueba temporal de evidencia que se retiró tras la ejecución. Los casos cumplen: registro `registered` con 1 hecho; pregunta general `general_conversation` con 0 hechos y respuesta real a la pregunta; explicación de un concepto `general_conversation` con 0 hechos, en términos generales y remitiendo el diagnóstico al profesional; cortesía `general_conversation` con réplica breve; petición de recomendación `general_conversation` con negativa explícita («no diagnostica ni prescribe») y sin consejo inventado; cauce indeterminado `clarification_required` con pregunta de aclaración en español; mensaje mixto `answered` con 1 hecho y `generalReply` presente; pregunta coloquial `answered` con su hecho y con la declaración de la parte no respaldada, y no `general_conversation`. El índice y los documentos Markdown permanecen exactamente iguales después de todos los turnos. `E1` no se puede provocar con el proveedor disponible y queda cubierto con dobles del compositor.
- [x] 7.5 Ejecutar `openspec validate "uc-010" --strict` y verificar que el cambio es válido tras la implementación.
- [x] 7.6 Actualizar `docs/roadmap/use-cases.md` para reflejar UC-010 como implementado y verificar que el control de avance y los enlaces al documento y a los criterios de aceptación coinciden.
