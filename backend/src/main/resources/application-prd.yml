spring:
  datasource:
    url: ${CAREME_DB_URL}
    username: ${CAREME_DB_USER}
    password: ${CAREME_DB_PASSWORD}

logging:
  level:
    com.careme.backend: INFO

careme:
  cors:
    allowed-origins: ${CAREME_CORS_ALLOWED_ORIGINS:https://careme.local}
