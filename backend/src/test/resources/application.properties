# The simulated assistant and the simulated composers are selected explicitly for
# the whole test suite, so it runs without network access and without a provider
# credential. The application's own default is the real assistant
# (`careme.llm.mode=openai` in src/main/resources/application.yml).
careme.llm.mode=fake
